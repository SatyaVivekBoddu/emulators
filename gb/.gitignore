# Build output
build/
build-*/
cmake-build-*/
out/
compile_commands.json

# Compiled objects / binaries
*.o
*.obj
*.a
*.so
*.dylib
*.dll
*.exe

# clangd / language server cache
.cache/
.clangd/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store

# ROMs — copyrighted (Tetris) and third-party test suites; never committed.
# Keep the folder itself present (via roms/.gitkeep) so the default ROM path still exists.
roms/**
!roms/.gitkeep
